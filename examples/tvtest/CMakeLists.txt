INCLUDE_DIRECTORIES(../../src ${INC_DIRS})
LINK_DIRECTORIES(../../src ${LINK_DIRS})
ADD_EXECUTABLE(tvtest tvtest.c)
TARGET_LINK_LIBRARIES(tvtest ${LIBUSBNAME} ${LIBTVNAME})
IF(NOT WIN32)
  INSTALL(TARGETS tvtest RUNTIME DESTINATION "/usr/local/bin")
ENDIF(NOT WIN32)
