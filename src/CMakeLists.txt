SET(SRCS trancevibe.c)
ADD_LIBRARY (trancevibe_static STATIC ${SRCS})
ADD_LIBRARY (trancevibe_dynamic MODULE ${SRCS})
#SET_TARGET_PROPERTIES (trancevibe_static trancevibe_dynamic PROPERTIES OUTPUT_NAME trancevibe)
INCLUDE_DIRECTORIES(/usr/include /usr/local/include)
FIND_LIBRARY (LIBUSB NAMES libusb PATHS /usr/lib /usr/local/lib)
IF (LIBUSB)
	TARGET_ADD_LIBRARY(libtrancevibe_static LIBUSB)
	TARGET_ADD_LIBRARY(libtrancevibe_dynamic LIBUSB)
ENDIF (LIBUSB)
